import { Button } from "@theme-ui/components";
import React from "react";

const Payment = () => {
  return (
    <Button
      variant="cta"
      sx={{
        width: "100%",
      }}
    >
      Pay
    </Button>
  );
};

export default Payment;
